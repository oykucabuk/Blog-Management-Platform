<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Articles</title>
    <link rel="stylesheet" href="/css/main.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <ul class="nav nav-pills nav-fill">
        <li class="nav-item">
            <a class="nav-link" aria-current="page" href="/about">Home</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/about">About</a>
          </li>
          <li class="nav-item">
            <a class="nav-link active" href="/articles">Articles</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/categories">Categories</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/articles/add">Add Article</a>
          </li>
      </ul>
</head>
<body>
    <div class="container mt-5">
        <h1>Articles</h1>
        
        
        <% if (errorMessage || !articles || articles.length === 0) { %>
            <div class="alert alert-danger" role="alert">
                <%= errorMessage || "No articles found." %>
            </div>
        <% } else { %>
            
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>Title</th>
                        <th>Author</th>
                        <th>Date</th>
                        <th>Category</th>
                    </tr>
                </thead>
                <tbody>
                    <% articles.forEach(article => { %>
                        <tr>
                            <td>
                                <a href="/article/<%= article.id %>">
                                    <%= article.title %>
                                </a>
                            </td>
                            <td><%= article.author %></td>
                            <td><%= new Date(article.minDateStr).toLocaleDateString() %></td>
                            <td>
                                <a href="/articles?category=<%= article.category %>">
                                    <%= article.category %> 
                                </a>
                            </td>
                        </tr>
                    <% }); %>
                </tbody>
            </table>
        <% } %>
    </div>
</body>
</html>